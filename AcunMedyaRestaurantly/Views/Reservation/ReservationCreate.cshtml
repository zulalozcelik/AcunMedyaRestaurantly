﻿@model AcunMedyaRestaurantly.Entities.Reservation
@{
    ViewBag.Title = "ReservationCreate";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="row page-titles mx-0">
    <div class="col p-md-0">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="javascript:void(0)">Anasayfa</a></li>
            <li class="breadcrumb-item active"><a href="javascript:void(0)">Rezervasyon Ekleme</a></li>
        </ol>
    </div>
</div>
<div class="container-fluid">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Rezervasyon Ekleme Formu</h4>
                <div class="basic-form">
                    <form method="post">

                        <div class="form-group">
                            <label>Ad - Soyad</label>
                            @Html.TextBoxFor(x => x.Name, new { @class = "form-control" })
                            <label>e-Posta</label>
                            @Html.TextBoxFor(x => x.Email, new { @class = "form-control" })
                            <label>Telefon</label>
                            @Html.TextBoxFor(x => x.Phone, new { @class = "form-control" })
                            <label>Açıklama</label>
                            @Html.TextBoxFor(x => x.Description, new { @class = "form-control" })
                            <label>Tarih</label>
                            @Html.TextBoxFor(x => x.ReservationDate, new { @class = "form-control" })
                            <label>Saat</label>
                            @Html.TextBoxFor(x => x.Time, new { @class = "form-control" })
                            <label>Kişi Sayısı</label>
                            @Html.TextBoxFor(x => x.GuestCount, new { @class = "form-control" })
                            <label>Rezervasyon Durumu</label>
                            @Html.DropDownListFor(x => x.ReservationStatus, (List<SelectListItem>)ViewBag.v, new { @class = "form-control" })
                        </div>

                        <button type="submit" class="btn btn-dark gradient-2">Ekle</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>